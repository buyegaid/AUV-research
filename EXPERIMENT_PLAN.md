# 完整实验流程设计

## 实验目标

从简单到复杂，逐步验证PI-ESO的有效性，隔离问题来源。

## 实验阶段

### 阶段1: 定点控制（Station-Keeping）

**脚本**: `test_station_keeping.m`

**测试用例**:
1. 无海流基线 (Vc=0) - 验证控制器基本功能
2. 恒定海流-SMC (Vc=0.3m/s) - 基线性能
3. 恒定海流-LESO (Vc=0.3m/s) - 标准ESO补偿
4. 恒定海流-PI-ESO (Vc=0.3m/s) - 物理信息ESO补偿

**关键指标**:
- 稳态位置误差 (m) - 最后100s的RMSE
- 稳态姿态误差 (°) - 最后100s的RMSE
- ESO估计均值 (N) - 验证ESO是否正确估计海流力

**预期结果**:
- 无海流: 位置误差 < 1m
- 有海流-SMC: 位置误差 > 5m（海流漂移）
- 有海流-LESO/PI-ESO: 位置误差 < 2m（ESO补偿）

---

### 阶段2: 直线轨迹跟踪

**脚本**: `test_straight_line.m`

**测试用例**:
1. 无海流基线
2. 恒定海流 (Vc=0.3m/s, 45°)
3. 恒定海流 (Vc=0.5m/s, 45°) - 更强扰动

**轨迹**: 从(0,0,10)到(100,0,10)，直线50m

**关键指标**:
- 横向误差RMSE (m)
- 航向误差RMSE (rad)
- 到达时间 (s)

**预期结果**:
- ESO方法横向误差显著小于SMC
- PI-ESO与LESO性能相近（恒定流场景）

---

### 阶段3: 时变海流定点控制

**脚本**: `test_station_keeping_timevarying.m`

**测试用例**:
1. Gauss-Markov海流 (tau_c=30s, sigma=0.1)
2. Gauss-Markov海流 (tau_c=50s, sigma=0.1)
3. Gauss-Markov海流 (tau_c=100s, sigma=0.1)

**目标**: 验证PI-ESO在时变海流下的优势

**关键指标**:
- 位置误差RMSE (m)
- ESO估计误差 vs 真实海流力
- PI-ESO vs LESO改善百分比

**预期结果**:
- PI-ESO在时变海流下优于LESO（Gauss-Markov先验发挥作用）
- tau_c参数匹配时性能最优

---

### 阶段4: 圆形轨迹跟踪（恒定海流）

**脚本**: `test_circular_trajectory.m`

**测试用例**:
1. 小半径 (R=50m, Vc=0.3m/s)
2. 中半径 (R=100m, Vc=0.3m/s)
3. 大半径 (R=300m, Vc=0.3m/s)

**关键指标**:
- 横向误差RMSE (m)
- 航向误差RMSE (rad)
- 控制输入RMS

**预期结果**:
- 小半径轨迹ESO改善更明显（曲率大，扰动影响大）

---

### 阶段5: 圆形轨迹跟踪（时变海流）

**脚本**: `test_circular_trajectory_timevarying.m`

**测试用例**:
1. R=100m, Gauss-Markov海流 (tau_c=30s)
2. R=100m, Gauss-Markov海流 (tau_c=50s)
3. R=100m, Gauss-Markov海流 (tau_c=100s)

**目标**: 最复杂场景，验证PI-ESO最终性能

**关键指标**:
- 横向误差RMSE (m)
- 航向误差RMSE (rad)
- PI-ESO vs LESO改善百分比

**预期结果**:
- PI-ESO显著优于LESO（时变海流+复杂轨迹）
- tau_c参数敏感性分析

---

## 实验执行顺序

1. **先运行阶段1** - 验证定点控制和ESO基本功能
2. **如果阶段1失败** - 调试控制器参数，不继续后续实验
3. **运行阶段2** - 验证直线跟踪
4. **运行阶段3** - 验证时变海流下PI-ESO优势
5. **运行阶段4-5** - 完整验证

## 成功标准

**阶段1必须满足**:
- 无海流位置误差 < 1m
- 有海流-LESO位置误差 < 有海流-SMC的50%

**阶段3必须满足**:
- PI-ESO位置误差 < LESO位置误差的90%（至少10%改善）

**如果阶段3失败**:
- 说明Gauss-Markov先验在当前实现下无效
- 需要重新评估方法或调整参数

## 参数调优策略

如果初步实验效果不佳，按以下顺序调优:

1. **ESO带宽** (omega0_base, omega0_max)
   - 增大带宽 → 更快响应，但噪声敏感
   - 减小带宽 → 更平滑，但响应慢

2. **SMC增益** (Kd, Ks)
   - 与ESO补偿协调
   - ESO补偿后可减小Ks（减少抖振）

3. **tau_c参数** (仅PI-ESO)
   - 匹配真实海流时间尺度
   - 尝试20s, 30s, 50s, 100s

4. **滤波参数** (meas_lpf_fc, z3_lpf_fc)
   - 平衡噪声抑制和响应速度
