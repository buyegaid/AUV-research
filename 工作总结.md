# XHY AUV 仿真工作总结

## 已完成工作

### 1. 项目文档
- ✅ **Readme.md** — 更新项目目标、系统架构、参数表、进度清单
- ✅ **CLAUDE.md** — 创建代码库指南，包含运行命令、架构说明、迁移任务

### 2. 控制器实现
- ✅ **controller/smc_6dof.m** — 6自由度滑模控制器（已存在，验证接口）
- ✅ **controller/thrust_allocation_xhy.m** — 推力分配逆映射（6-DOF力/力矩 → 5推进器RPM）
  - 使用伪逆求解（横滚通道不可控）
  - 推力模型：T = ρ·D_prop^4·KT·|n|·n
  - 包含饱和限幅和诊断输出

### 3. 主仿真循环
- ✅ **main_loop_xhy.m** — 新建XHY模型专用仿真循环
  - 集成6-DOF SMC + ESO闭环控制
  - 使用`xhy()`动力学模型（替代remus100）
  - 3D ALOS制导 → SMC控制 → 推力分配 → 动力学更新
  - ESO扰动估计与补偿
  - 完整历史数据记录

### 4. 测试脚本
- ✅ **test_xhy_smc_eso.m** — 对比测试脚本（SMC vs SMC+ESO）

### 5. 工具函数
- ✅ **Lib/ssa.m** — 角度归一化到[-π, π]
- ✅ **Lib/sat.m** — 饱和限幅函数
- ✅ **Lib/rk4.m** — 四阶龙格-库塔积分器
- ✅ **Lib/LOSobserver.m** — LOS制导信号滤波器

## 系统架构

```
test_xhy_smc_eso.m (测试入口)
    ↓
main_loop_xhy.m (主循环)
    ├─→ my_ALOS3D.m (3D制导)
    ├─→ smc_6dof.m (6-DOF SMC控制器)
    ├─→ vec_leso_update_adv.m (ESO扰动估计)
    ├─→ thrust_allocation_xhy.m (推力分配)
    └─→ xhy.m (5推进器动力学模型)
```

## 关键参数

### SMC参数 (在main_loop_xhy.m中)
```matlab
smc_params.lambda = [0.5; 0.5; 0.5; 0.3; 0.3; 0.3];  % 滑模面斜率
smc_params.Kd = [5; 5; 5; 2; 2; 2];                  % 线性增益
smc_params.Ks = [2; 2; 2; 1; 1; 1];                  % 切换增益
smc_params.phi_b = [0.1; 0.1; 0.1; 0.05; 0.05; 0.05]; % 边界层厚度
smc_params.rho_eso = [0.8; 0.8; 0.8; 0.5; 0.5; 0.5]; % ESO补偿系数
```

### ESO参数 (在get_params.m中)
```matlab
params.eso.omega0_base = 3;      % 基础带宽
params.eso.omega0_max = 5;       % 最大带宽（自适应）
params.eso.alpha1 = 0.6;         % fal函数参数
params.eso.alpha2 = 0.6;
params.eso.alpha3 = 0.5;
params.eso.delta = 0.02;         % fal函数线性区宽度
```

## 运行方法

```matlab
% 在MATLAB中运行
cd c:\Users\sixuh\Documents\B_matlab_ws\AUV\research
addpath('.', './Lib', './heading/lib', './heading/controller', './controller', './model');

% 运行对比测试
test_xhy_smc_eso
```

## 待完成工作

### 1. 参数调优
- [ ] SMC增益调整（lambda, Kd, Ks, phi_b）
- [ ] ESO带宽优化（omega0_base, omega0_max）
- [ ] 推力分配权重优化（当前使用伪逆，可考虑加权伪逆）

### 2. 验证测试
- [ ] 直线轨迹跟踪精度
- [ ] 螺旋/圆形轨迹跟踪
- [ ] 海流扰动鲁棒性（静态/动态海流）
- [ ] 对比分析：SMC vs SMC+ESO性能提升

### 3. 可能的改进
- [ ] 自适应SMC增益（根据跟踪误差动态调整）
- [ ] 推力分配优化（考虑能耗最小化）
- [ ] 增加横滚稳定控制（当前横滚通道不可控）
- [ ] 添加推进器动态模型（当前假设瞬时响应）

## 文件清单

### 新建文件
1. `controller/thrust_allocation_xhy.m` — 推力分配
2. `main_loop_xhy.m` — XHY主循环
3. `test_xhy_smc_eso.m` — 测试脚本
4. `Lib/ssa.m` — 角度归一化
5. `Lib/sat.m` — 饱和函数
6. `Lib/rk4.m` — RK4积分器
7. `Lib/LOSobserver.m` — LOS滤波器
8. `CLAUDE.md` — 代码库指南

### 更新文件
1. `Readme.md` — 项目文档

### 已存在（未修改）
1. `model/xhy.m` — 动力学模型
2. `controller/smc_6dof.m` — SMC控制器
3. `vec_leso_update_adv.m` — ESO观测器
4. `heading/lib/my_ALOS3D.m` — 3D制导
5. `heading/lib/get_params.m` — 参数配置

## 注意事项

1. **坐标系约定**
   - 惯性系：NED（北东地）
   - 船体系：前右下
   - 角度单位：弧度

2. **推力分配限制**
   - 横滚通道不可控（5推进器无法独立控制横滚）
   - 使用伪逆求解，可能存在次优解

3. **ESO初始化**
   - 首次迭代a_known=0，可能导致初始瞬态
   - 建议预热几个时间步后再启用ESO补偿

4. **仿真时长**
   - 当前设置500秒，50000次迭代
   - 可根据需要调整（修改main_loop_xhy.m第16行）

## 下一步建议

1. **立即测试**：运行`test_xhy_smc_eso.m`，检查是否有语法错误或缺失依赖
2. **参数调优**：根据仿真结果调整SMC和ESO参数
3. **性能评估**：分析跟踪误差、控制输入、ESO估计精度
4. **文档完善**：记录最优参数和性能指标

---
生成时间：2026-05-13
